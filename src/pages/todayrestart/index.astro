---
import LandingLayout from '@/layouts/LandingLayout.astro'
import Hero from '@/components/landing/Hero.astro'
import FeatureShowcase from '@/components/landing/FeatureShowcase.astro'
import FeatureGrid from '@/components/landing/FeatureGrid.astro'
import FeatureList from '@/components/landing/FeatureList.astro'
import PricingTable from '@/components/landing/PricingTable.astro'
import CTAButton from '@/components/landing/CTAButton.astro'
import { hero, featureShowcases, featureGridItems, moreFeatures, pricingTiers } from '@/data/todayrestart'
import { apps } from '@/config'

const app = apps.find(a => a.id === 'todayrestart')!
---

<LandingLayout
  title={`${app.name} - ${app.tagline}`}
  description={app.description}
  accentColor={app.accentColor}
>
  <!-- Hero -->
  <Hero data={hero} appIcon={app.icon} />

  <!-- Feature Showcases (alternating) -->
  {featureShowcases.map((showcase, i) => (
    <FeatureShowcase data={showcase} alt={i % 2 === 1} />
  ))}

  <!-- Feature Grid -->
  <FeatureGrid
    items={featureGridItems}
    title="Designed for Real Life"
    subtitle="Every feature built to reduce pressure and encourage return."
  />

  <!-- Feature List -->
  <FeatureList
    items={moreFeatures}
    title="All Features"
    subtitle="A complete toolkit for sustainable habit building."
  />

  <!-- Pricing -->
  <PricingTable
    tiers={pricingTiers}
    title="Simple Pricing"
    subtitle="Start free. Remove ads when you're ready."
  />

  <!-- Final CTA -->
  <section class="landing-section">
    <div class="landing-inner final-cta">
      <h2 data-reveal="fade-up">Ready to Start Again?</h2>
      <p data-reveal="fade-up">Download Today Restart and build habits that last, one minute at a time.</p>
      <div class="hero-ctas" data-reveal="fade-up">
        <CTAButton href="https://apps.apple.com/kr/app/%EC%98%A4%EB%8A%98-%EB%8B%A4%EC%8B%9C-%EB%82%98%EB%A5%BC-%EB%A7%8C%EB%93%9C%EB%8A%94-%EC%9E%91%EC%9D%80-%EC%8A%B5%EA%B4%80/id6758337278" variant="primary">Download Free</CTAButton>
        <CTAButton href="https://apps.apple.com/kr/app/%EC%98%A4%EB%8A%98-%EB%8B%A4%EC%8B%9C-%EB%82%98%EB%A5%BC-%EB%A7%8C%EB%93%9C%EB%8A%94-%EC%9E%91%EC%9D%80-%EC%8A%B5%EA%B4%80/id6758337278" variant="secondary">View on App Store</CTAButton>
      </div>
    </div>
  </section>
</LandingLayout>
